import { SidePanelController } from "./sidepanel/SidePanelController.js";

document.addEventListener("DOMContentLoaded", () => {
  const controller = new SidePanelController();
  controller.init();
});
